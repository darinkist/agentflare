# OpenCode integration

Reserved extension point for a future OpenCode integration. No implementation is
installed yet.

When added, OpenCode-specific observation and normalization belong here. The
AgentFlare daemon and its normalized event contract should remain unchanged.
