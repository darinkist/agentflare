# Direct-LED patch source

`km16pro_direct_led.s` is the single supported Direct-LED patch. It is built by
`../../tools/km16pro_build_direct_led.py` from a verified stock backup and
adds per-LED control without replacing the stock firmware.

There are no patch versions. Do not add `v1`/`v2` variants here.
